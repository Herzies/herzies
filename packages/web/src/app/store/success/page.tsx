export default function StoreSuccessPage() {
  return (
    <main className="w-full max-w-[360px] px-6 flex flex-col gap-4">
      <div className="text-center mb-2">
        <h1 className="text-lg text-purple mb-1">thanks!</h1>
        <p className="text-xs text-text-dim">
          your coins are on the way — you can close this tab and return to
          herzies. your balance updates automatically.
        </p>
      </div>
    </main>
  );
}
